apiVersion: skaffold/v4beta1
kind: Config
metadata:
  name: octopilot-mcp
build:
  artifacts:
    # Python package (src/octopilot_mcp).
    # detect-contexts picks up language=python from pyproject.toml.
    - image: octopilot-mcp
      context: .

